spring:
  datasource:
    url: jdbc:h2:mem:testdb
#    username: user
#    password: password
    initialization-mode: always
    schema: schema.sql
    data: data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

logging:
  level:
    ROOT: info